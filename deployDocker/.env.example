# PostgreSQL Database Configuration
POSTGRES_DB=bmi_health_db
POSTGRES_USER=bmi_user
POSTGRES_PASSWORD=Change_Me_Strong_Password_123!

# Backend Database Connection
DATABASE_URL=postgresql://bmi_user:Change_Me_Strong_Password_123!@postgres-db:5432/bmi_health_db

# Backend Server Configuration
PORT=3000
NODE_ENV=production

# CORS Configuration (for production, use your domain or ALB URL)
FRONTEND_URL=http://localhost

# Container Names (used by scripts)
CONTAINER_DB=postgres-db
CONTAINER_BACKEND=backend-api
CONTAINER_FRONTEND=frontend-web

# Docker Network
NETWORK_NAME=bmi-health-network

# Docker Volume
VOLUME_NAME=postgres-data
